# dsh-session-trash bundle patch: keep the stock workspace browser and add the
# trash panel through the supported sidebar.footer.action extension point.
#
# Install with: dsh plugin --profile web add <path-to-this-directory>
- insert:
    - id: session-trash
      name: '@dsh-external/dsh-session-trash'
